package chapter2.operators;

public class Operators {
    /*operator - a special symbol that can be applied to a set of cariables, values, or literals aka "operands", that
    return a result.

    order of precedence:

    x++, x--,
    ++x, --x,
    +, -, !,
    *, /, %
    <<, >>, >>>
    <, >,  <= >=, instanceof
    ==, !=
    &, ^, |
    &&, ||
    ternary operators: expression ? iftrue : iffalse
    assignment operators: =, += -=

     */
    public static void main(String[] args){



    }

}
